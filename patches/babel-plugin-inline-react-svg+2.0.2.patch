diff --git a/node_modules/babel-plugin-inline-react-svg/lib/index.js b/node_modules/babel-plugin-inline-react-svg/lib/index.js
index 1d01463..526b421 100644
--- a/node_modules/babel-plugin-inline-react-svg/lib/index.js
+++ b/node_modules/babel-plugin-inline-react-svg/lib/index.js
@@ -37,20 +37,17 @@ var _default = (0, _helperPluginUtils.declare)(function (_ref) {
     var IS_EXPORT = _ref2.IS_EXPORT,
       EXPORT_FILENAME = _ref2.EXPORT_FILENAME,
       SVG_NAME = _ref2.SVG_NAME,
-      SVG_CODE = _ref2.SVG_CODE,
-      SVG_DEFAULT_PROPS_CODE = _ref2.SVG_DEFAULT_PROPS_CODE;
-    var namedTemplate = "\n      var SVG_NAME = function SVG_NAME(props) { return SVG_CODE; };\n      ".concat(SVG_DEFAULT_PROPS_CODE ? 'SVG_NAME.defaultProps = SVG_DEFAULT_PROPS_CODE;' : '', "\n      ").concat(IS_EXPORT ? 'export { SVG_NAME };' : '', "\n    ");
-    var anonymousTemplate = "\n      var Component = function (props) { return SVG_CODE; };\n      ".concat(SVG_DEFAULT_PROPS_CODE ? 'Component.defaultProps = SVG_DEFAULT_PROPS_CODE;' : '', "\n      Component.displayName = 'EXPORT_FILENAME';\n      export default Component;\n    ");
+      SVG_CODE = _ref2.SVG_CODE;
+    var namedTemplate = "\n      var SVG_NAME = function SVG_NAME(props) { return SVG_CODE; };\n      ".concat(IS_EXPORT ? 'export { SVG_NAME };' : '', "\n    ");
+    var anonymousTemplate = "\n      var Component = function (props) { return SVG_CODE; };\n      Component.displayName = 'EXPORT_FILENAME';\n      export default Component;\n    ";
     if (SVG_NAME !== 'default') {
       return template(namedTemplate)({
         SVG_NAME: SVG_NAME,
-        SVG_CODE: SVG_CODE,
-        SVG_DEFAULT_PROPS_CODE: SVG_DEFAULT_PROPS_CODE
+        SVG_CODE: SVG_CODE
       });
     }
     return template(anonymousTemplate)({
       SVG_CODE: SVG_CODE,
-      SVG_DEFAULT_PROPS_CODE: SVG_DEFAULT_PROPS_CODE,
       EXPORT_FILENAME: EXPORT_FILENAME
     });
   };
@@ -106,31 +103,11 @@ var _default = (0, _helperPluginUtils.declare)(function (_ref) {
         EXPORT_FILENAME: exportFilename
       };
 
-      // Move props off of element and into defaultProps
-      if (svgCode.openingElement.attributes.length > 1) {
-        var keepProps = [];
-        var defaultProps = [];
-        svgCode.openingElement.attributes.forEach(function (prop) {
-          if (prop.type === 'JSXSpreadAttribute') {
-            keepProps.push(prop);
-          } else if (prop.value.type === 'JSXExpressionContainer') {
-            var objectExpression = t.objectExpression(prop.value.expression.properties);
-            defaultProps.push(t.objectProperty(t.identifier(prop.name.name), objectExpression));
-          } else {
-            defaultProps.push(t.objectProperty(t.identifier(prop.name.name), prop.value));
-          }
-        });
-        svgCode.openingElement.attributes = keepProps;
-        opts.SVG_DEFAULT_PROPS_CODE = t.objectExpression(defaultProps);
-      }
+      // Attributes remain on the SVG element; the {…props} spread added by transformSvg
+      // (always last) lets callers override them. Extracting attributes to defaultProps
+      // triggers React 18 deprecation warnings on function components, so we skip it.
       var svgReplacement = buildSvg(opts);
-      if (opts.SVG_DEFAULT_PROPS_CODE) {
-        var _path$replaceWithMult = path.replaceWithMultiple(svgReplacement);
-        var _path$replaceWithMult2 = _slicedToArray(_path$replaceWithMult, 1);
-        newPath = _path$replaceWithMult2[0];
-      } else {
-        newPath = path.replaceWith(svgReplacement);
-      }
+      newPath = path.replaceWith(svgReplacement);
       file.get('ensureReact')();
       file.set('ensureReact', function () {});
     }
diff --git a/node_modules/babel-plugin-inline-react-svg/lib/transformSvg.js b/node_modules/babel-plugin-inline-react-svg/lib/transformSvg.js
index c9bcfb0..d50498b 100644
--- a/node_modules/babel-plugin-inline-react-svg/lib/transformSvg.js
+++ b/node_modules/babel-plugin-inline-react-svg/lib/transformSvg.js
@@ -47,10 +47,11 @@ var _default = function _default(t) {
         // <svg stroke-width="5">
         // to
         // <svg strokeWidth="5">
-        // don't convert any custom data-* or aria-* attributes just wrap in quotes
-        if (/^data-|^aria-/.test(originalName.name)) {
-          originalName.name = "'".concat(originalName.name, "'");
-        } else {
+        // data-* and aria-* are left as-is: React natively supports these hyphenated
+        // attributes on DOM/SVG elements. Previously they were wrapped in quotes for
+        // use as defaultProps object keys, but since we keep attributes inline on the
+        // JSX element the quoted form would produce an invalid attribute name.
+        if (!/^data-|^aria-/.test(originalName.name)) {
           originalName.name = (0, _camelize.hyphenToCamel)(originalName.name);
         }
       }
